#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
ROOT_DIR=$(cd "$SCRIPT_DIR/.." && pwd)

# Open an interactive shell in the app container
exec docker compose -f "$ROOT_DIR/docker-compose.yml" exec app bash
