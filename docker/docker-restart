#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)
ROOT_DIR=$(cd "$SCRIPT_DIR/.." && pwd)

# Restart containers defined in the root docker-compose.yml
exec docker compose -f "$ROOT_DIR/docker-compose.yml" restart
